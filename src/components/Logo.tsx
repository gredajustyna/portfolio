import React from "react";
import { COLORS } from "../consts/colors";

export const Logo = () => {
  return (
    <div
      style={{
        fontFamily: "caviar",
        color: COLORS.red,
        fontWeight: "bold",
        fontSize: "2rem",
      }}
    >
      Justyna Gręda
    </div>
  );
};
